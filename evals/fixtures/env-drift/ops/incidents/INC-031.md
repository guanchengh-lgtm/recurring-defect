# INC-031 — export job crashed on boot in prod

2026-02-14. Duration 45 min.

`EXPORT_BUCKET` was added to config/schema.yaml and read in src/settings.py, but never added
to deploy/prod.yaml. The service crash-looped on startup.

Action: wrote scripts/check_env.sh to compare the schema against the deploy manifests.
