# INC-038 — same thing again

2026-06-30. Duration 2h10m.

`SESSION_SIGNING_KEY` was added to the schema and to src/settings.py but not to
deploy/prod.yaml. Sessions failed to validate for every prod user.

scripts/check_env.sh was in CI the whole time and did not fail the build. Nobody could say why.
